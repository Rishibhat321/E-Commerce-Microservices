spring.application.name=discovery


server.port=8761

# Eureka is the server itself. It does not need to register itself.
eureka.client.register-with-eureka=false

# fetching the list of all available services. Here, eureka itself is the registry.
eureka.client.fetch-registry=false